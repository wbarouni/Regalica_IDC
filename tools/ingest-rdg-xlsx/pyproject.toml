[project]
name = "ingest-rdg-xlsx"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["pandas>=2.0", "openpyxl>=3.1"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]
include = ["ingest.py"]
