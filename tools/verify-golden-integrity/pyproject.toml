[project]
name = "regflow-verify-golden-integrity"
version = "0.0.0"
description = "Verify checksums and structure of the tenant pilote golden baseline (Phase 1+)"
readme = "README.md"
requires-python = ">=3.12"
authors = [{ name = "ALGORIA Factory" }]
license = { text = "UNLICENSED" }
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
