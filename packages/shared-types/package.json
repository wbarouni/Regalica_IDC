{
  "name": "@regalica/shared-types",
  "version": "0.0.0",
  "private": true,
  "description": "Shared TypeScript types across api, chatbot-node, and frontend",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "echo \"(shared-types) no tests yet\" && exit 0"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
