{
  "name": "@regalica/rdg-schema",
  "version": "0.0.0",
  "private": true,
  "description": "RDG rule AST DSL — zod schemas (TS) mirrored by pydantic models (PY) in Phase 1",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "echo \"(rdg-schema) AST schemas arrive in Phase 1\" && exit 0"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
