/**
 * RDG rule AST DSL — placeholder for Phase 1.
 *
 * In Phase 1 this package will host the zod schema for the 4 611 rules in
 * `tests/fixtures/rdg.xlsx`. A parallel pydantic model with the same shape
 * will live in `apps/chatbot-py/app/rdg/` so agents and evaluator stay in sync.
 *
 * Until then, this placeholder unblocks workspace resolution for
 * `@regalica/rdg-schema`.
 */

export const RDG_SCHEMA_VERSION = '0.0.0-placeholder' as const;
