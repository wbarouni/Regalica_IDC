{
  "name": "@regflow/ui",
  "version": "0.0.0",
  "private": true,
  "description": "UI components — Edition One design system (Phase 5)",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "echo \"(ui) Edition One components arrive in Phase 5\" && exit 0",
    "lint": "echo \"(ui) lint skeleton\" && exit 0"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "@types/node": "^20.17.6"
  }
}
