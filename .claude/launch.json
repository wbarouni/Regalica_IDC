{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "REGFlow — Frontend (Vite)",
      "runtimeExecutable": "pnpm",
      "runtimeArgs": ["--filter", "@regflow/web", "dev"],
      "port": 5173,
      "cwd": "${workspaceFolder}",
      "autoPort": true,
      "env": {
        "NODE_ENV": "development"
      }
    }
  ]
}
